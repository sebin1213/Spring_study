#spring:
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/jpashop
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver  # 데이터베이스 커넥션과 관련된 설정
#  jpa:
#    hibernate:
#      ddl-auto: create # 자동으로 테이블을 만들어주는 모드, 애플리케이션 실행시점에 엔티티정보를 보고 테이블을 다 지우고 다시생성
#    properties:
#      hibernate:
#        # show_sql: true   # 스프링부트 메뉴얼에서 찾아보면서 배우자
#        format_sql: true
#logging.level:
#  org.hibernate.SQL: debug # 하이버네이트가 남기는 로그가 모두 디버그 모드로 해서 실행, 생성하는 sql이 로그로 다 보임
## org.hibernate.type: trace